""" The module containing all the standard constants and enums for the atlases """
import numpy as np

X, Y, Z = 0, 1, 2
XVECTOR = np.array([1, 0, 0])
YVECTOR = np.array([0, 1, 0])
ZVECTOR = np.array([0, 0, 1])
XYZ = slice(0, 3)
